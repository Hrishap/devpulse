# DevPulse VS Code Extension

Captures lightweight editor activity and sends batched events to the DevPulse backend.

## Local development

```bash
npm install
npm run compile
```

Press `F5` in VS Code, register in the DevPulse dashboard, and set `devpulse.apiKey` in VS Code settings.
